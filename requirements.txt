# Install: pip install -r requirements.txt
# Prefer: pip install -e ".[dev]" from repo root (uses pyproject.toml)

pydantic>=2.0
polars>=1.0
pyarrow>=14.0
duckdb>=1.0
pytz>=2024.1
pandas>=2.0
numpy>=1.26
fastapi>=0.110
uvicorn[standard]>=0.27
pytest>=8.0
httpx>=0.27
pyyaml>=6.0
